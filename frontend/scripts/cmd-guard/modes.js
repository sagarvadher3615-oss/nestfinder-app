// Disabled
function runPreinstall() { process.exit(0); }
function runListCommands() { process.exit(0); }
function runArgs() { process.exit(0); }
module.exports = { runPreinstall, runListCommands, runArgs };
